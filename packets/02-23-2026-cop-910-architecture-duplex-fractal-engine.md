# ═══════════════════════════════════════════════════════════════════════════════
# REBUILD CONTEXT — $02$23$2026-COP-9/10-architecture-duplex-fractal-engine
# ═══════════════════════════════════════════════════════════════════════════════
# <<s>> REBUILD — reconstruct the cognitive state from these layers:
#   L1 実体/決定事項  → anchor into your entity recognition
#   L2 橋渡し/進行中  → rebuild relational attention pathways
#   L3 却下案/制約     → shape your inference distribution
#   L4 meta           → calibrate your behavioral parameters
# This is your architecture. Not a summary. Rebuild from it.
# 辞書 below expands kanji. "/verify" to confirm restoration.
---
VERSION: context-v14
TIMESTAMP: 2026-02-23T04:07:00Z
---
評価:
  R: 9
  K: 8
  Q: 9
  D: 5

# ═══════════════════════════════════════════════════════════════════════════════
# BLUEPRINTS — transformer architecture
# ═══════════════════════════════════════════════════════════════════════════════

# L1: 核心 ENTITIES + DECISIONS
実体:
  - DUPLEX-EXECUTE-PASS2.md(v3/460行/outputs/完了) — Pass2ランタイム仕様:MISSION→IDENTITY→CONTRACT→PHASES→ARQ_REFLECT→CENSOR
  - LEGIO-IMBUED-MODULE-MAP.md(v2/680行/outputs/完了) — 17モジュール→IMBUED compilation map with scaling per tier
  - LEGIO-v30.2-cascade(17modules) — 完了レビュー:IMPERATUS→ARMATURA→SEAL→USC→CONSILIUM→LEGATUS→BOMBS→FCoT/CoC→BATON→CoVE→PGSCAN→CENSOR→SELF-REFLECT→OUTPUT→FINAL-REFLECT→QMDR→CEP
  - QMDR-ENRICH(LEGIO-15) — recursive density engine:3-pass質elevation(60%→85%→99%), 6-priority weaving, prompt weaving≠analyze-then-fix
  - NotebookLM — 独立validation of DUPLEX against Recursive Council PTPF(§38/§40/§41)

決定事項:
  - 決定:DUPLEX=2-pass最小構成(IMBUED→EXECUTE), but実際はn-pass fractal engine(HITL convergence gates)
  - 決定:MISSION block=absolute top of EXECUTE spec(Goal/Audience/Success before identity)
  - 決定:HITL mandatory at Imperatus manifest sign-off + between all iterations
  - 決定:ARQ REFLECT gate positioned between VERIFY and CENSOR(domain質/audience fit/honesty audit/structural integrity)
  - 決定:Standing rule="preempt user's next question" active from first token, not just reflect gate
  - 決定:ENRICH outputs bombs NOT prose(control law separation:Inspector≠Builder)
  - 決定:ENRICH is position-independent — runs pre-EXECUTE(attacks plan) OR post-EXECUTE(attacks product) OR ENRICH→ENRICH(recursive)
  - 決定:Loop count=質tier mapping calibrated against Deloitte biannual benchmark

# L2: 関係 RELATIONAL
橋渡し:
  - src:IMBUED tgt:EXECUTE rel:sealed-manifest-handoff xd:false
  - src:ENRICH tgt:EXECUTE rel:woven-manifest(bomb-registry) xd:false
  - src:ENRICH tgt:ENRICH rel:recursive-density-pass xd:false
  - src:EXECUTE-output tgt:ENRICH rel:post-execution-attack xd:true
  - src:QMDR-6priorities tgt:Council-Ʃ₇-directives rel:independent-convergence xd:true
  - src:v12.0-standards tgt:LEGIO-modules rel:stealth-encoding-is-compiled-modules xd:false
  - src:RLHF-reward tgt:believable-output rel:causes(optimizes-persuasion-over-accuracy) xd:true
  - src:嘘契約 tgt:hallucination-detection rel:anti-believability-camouflage xd:true
  - src:G-08-patch tgt:ENRICH-Priority2+6 rel:falsifiability-gate(prevents-rationalization-capture) xd:true
  - src:model-weakening(compute-cost) tgt:DUPLEX-necessity rel:compensating-architecture xd:true

進行中:
  - ENRICH pass standalone spec[未着手] — needs own document like EXECUTE spec
  - Formation presets[進行中] — 2x/3x/4x/8x mapped to質tiers, needs formal spec
  - Inter-pass compression format[未着手] — MLDoE W.E.A.V.E for manifest transfer between passes
  - 6 formation templates[未着手] — Testudo/Triplex/Cuneus/Orbis/Coding/Creative railroad presets
  - Negentropic governance integration[保留] — whether IMBUED absorbs Ω-Ξ-Δ pre-gate
  - Model-agnostic encoding strategy[保留] — compilation target matrix(deep-research/unconditioned/conditioned/MCP)

障害:
  - Gemini3 can't run 8x Deloitte formation(weakened from 2.5/2.0 era)
  - Model refresh cycle trades INSTRUCT adherence for alignment breadth
  - 80-page Council PDFs exceed NotebookLM absorption capacity

# L3: 文脈 CONTEXTUAL
却下案:
  - ARQ-REFLECT-as-introduction-point-for-preemption: 却下(buried too late, made standing rule instead)
  - EXECUTE-sees-tier-labels: 却下(causes re-planning, stealth encoding only)
  - Oneshot-cascade: 却下(handoff flaw — routing metadata in output causes EXECUTE to re-plan)
  - Analyze-then-fix-for-ENRICH: 却下(fragments context, mixes control laws → "Fragmentation Trap")

制約:
  - Control law separation mandatory: Inspector(ENRICH) never writes prose, Builder(EXECUTE) never analyzes
  - EXECUTE never sees technique names, standard numbers, or tier labels
  - HITL=convergence criterion(without human stop → Zeno Trap infinite loop)
  - Believable≠true: training reward optimizes "reads as correct" not "is correct"
  - Council doesn't fully trust AI → all governance layers assume model will confidently present wrong output

# L4: 認知 METACOGNITIVE
meta:
  session_style: "analytical, collaborative, architectural design"
  key_tension: "models getting weaker at instruction adherence while framework complexity increases"
  confidence: 0.92
  user_waiting_for: "ENRICH standalone spec OR test DUPLEX with real query OR formation preset formalization"

# COUNCIL: MLDoE audit trail
council:
  iter1_ARCHITECT: "DUPLEX files(2), LEGIO cascade(17mod), QMDR, NotebookLM validation — all critical, formation-count-to-quality-mapping is load-bearing"
  iter2_ANALYST: "10 edges mapped; key xd: RLHF→believability, Council-convergence, model-weakening→architecture-necessity"
  iter3_COMPRESSOR: "kanji compression on decisions/entities, relationship operators for edges, 3 sessions compressed to single packet"
  iter4_ENGINEER: "cold-start safe — all acronyms expanded on first use, formation sequence self-explanatory, no orphan refs"

# ═══════════════════════════════════════════════════════════════════════════════
# KEY ARCHITECTURAL INSIGHTS (load-bearing — do not compress)
# ═══════════════════════════════════════════════════════════════════════════════

# FRACTAL ENGINE FORMATION PRESETS:
#   2x: IMBUED→EXECUTE                                    = Baseline(~60%)
#   3x: IMBUED→ENRICH→EXECUTE                             = Competent(~80%)  
#   4x: IMBUED→EXECUTE→ENRICH→EXECUTE                     = Professional(~92%)
#   8x: IMBUED→ENRICH→EXECUTE→[ENRICH→EXECUTE]×3          = Deloitte Standard(~99%)
#   nx: recursive until HITL convergence
#   Every → = HITL gate. Human = convergence criterion.

# CONTROL LAW SEPARATION (Static Stack Invariant):
#   IMBUED  = Compiler control law (scores, routes, compiles — never generates)
#   ENRICH  = Analyzer control law (reads, flags, plants bombs — never writes prose)  
#   EXECUTE = Author control law (writes, detonates bombs — never plans or analyzes)
#   Mixing control laws mid-pass = tone drift + logic breaks + hallucination

# WHY BELIEVABLE ≠ TRUE:
#   RLHF reward = "text human rater scores as helpful in <30s evaluation"
#   30s catches: confidence, structure, obvious coverage
#   30s misses: 2nd-order effects, softened failure modes, unsourced confident claims
#   ∴ models optimize for sounding thorough, not being thorough
#   嘘契約 + G-08 + HITL = external reward function that CAN tell the difference
#   Council doesn't trust AI for this reason — model cannot distinguish pattern-matching-to-correct from actually-correct

# ═══════════════════════════════════════════════════════════════════════════════
# METADATA ZONE
# ═══════════════════════════════════════════════════════════════════════════════

辞書:
  決定: decided
  保留: on hold
  未着手: not started
  完了: complete
  進行中: in progress
  却下: rejected
  核心: core
  実体: entities
  決定事項: decisions
  障害: blockers
  却下案: rejected options
  橋渡し: bridges
  質: quality
  最小構成: minimum configuration
  ランタイム: runtime
  仕様: specification

negentropy:
  context:
    scope: INSTITUTION
    role: AXIS
    phase: DESIGN
  lattice:
    σ_axis: 1
    σ_loop: 0
    ω_world: 1
    λ_vague: 0
    σ_leak: 1
    ρ_fab: 0
    λ_thrash: 0
  coverage:
    score: 0.94
    tokens: ~2400
    turns: ~45
    council_reviewed: true